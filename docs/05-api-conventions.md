# Соглашения по API

- только JSON  
- все энд-поинты начинаются с `/api/<app>/`  
- пагинация: `?page=2`  
- сортировка: `?ordering=-created_at`  
- фильтры: query-params, не тело запроса  
- slug-поля вместо id для публичных постов